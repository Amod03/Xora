[build]
  command = "npm install --legacy-peer-deps && npm run build"
  publish = "dist"  # ← change this if you're using something like "build" or "out"

[build.environment]
  NODE_VERSION = "18.17.1"
